name: Unit tests for Vizro

defaults:
  run:
    working-directory: vizro-core

on:
  push:
    branches: [main]
  pull_request:
    branches:
      - "main"

concurrency:
  group: test-unit-${{ github.head_ref }}

env:
  PYTHONUNBUFFERED: "1"
  FORCE_COLOR: "1"

jobs:
  test-unit-vizro-core:
    name: test-unit-vizro-core on Py${{ matrix.python-version }} ${{ matrix.label }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        #python-version: ["3.8", "3.9", "3.10", "3.11"]
        include:
          - python-version: "3.11"
            hatch-env: all.py3.11
            label: lower
          - python-version: "3.11"
            hatch-env: all.py3.11
            label: lower

        # write matrix out manually with var for hatch env
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Hatch
        run: pip install --upgrade hatch


      - name: List dependencies
        run: hatch run all.py${{ matrix.hatch-env }}:pip freeze

      - name: Run unit tests
        run: hatch run all.py${{ matrix.hatch-env }}:test-unit-coverage
