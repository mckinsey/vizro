name: PyCafe Dashboards in CI

# defaults:
# run:
#   working-directory: vizro-core # but could be any folder

on:
  push:
    branches: [main]
  pull_request:
    branches:
      - main

env:
  PYTHON_VERSION: "3.11"

jobs:
  setup-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r tools/tools_requirements.txt
          pip install hatch
      - name: "Build vizro core package"
        run: |
          cd vizro-core
          hatch build
      - name: "Upload Artifact"
        uses: actions/upload-artifact@v4
        with:
          name: pip
          path: vizro-core/dist/*.whl
          retention-days: 5

      - name: Fetch latest commit from target branch
        run: |
          # git fetch origin ${{ github.ref_name }}
          latest_commit=${{ github.sha }}
          echo "COMMIT_HASH=$latest_commit" >> $GITHUB_ENV
          run_id=${{ github.run_id }}
          echo "RUN_ID=$run_id" >> $GITHUB_ENV

      - name: Display latest commit
        run: |
          echo "Latest commit on target branch: ${{ env.COMMIT_HASH }}"
          echo "Latest run id: ${{ env.RUN_ID }}"

      - name: Run Py.Cafe tool
        run: |
          python tools/pycafe/create_py_cafe_url.py

      - name: Create Check Run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl -L -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN"\
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/${{ github.repository }}/check-runs \
            -d '{"name":"A Mighty Test", "head_sha":"${{ github.event.pull_request.head.sha }}", "status":"in_progress","output":{"title":"A Mighty Summary","summary":"","text":""}}'

  # test_something:
  #   runs-on: ubuntu-latest
  #   permissions:
  #     checks: write
  #     contents: read
  #   steps:
  #     - uses: actions/checkout@v1
  #     - uses: setup-build
  #       id: test
  #     - uses: LouisBrunner/checks-action@v2.0.0
  #       if: always()
  #       with:
  #         token: ${{ secrets.GITHUB_TOKEN }}
  #         name: Test XYZ
  #         conclusion: ${{ job.status }}
  #         output: |
  #           {"summary":"${{ steps.test.outputs.summary }}"}

# why not build wheel now?
# see why not direct again?
