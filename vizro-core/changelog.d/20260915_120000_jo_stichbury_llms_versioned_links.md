### Fixed

- Fixed the links on the "Vizro for LLMs" documentation page so they resolve to the documentation version being viewed (for example `latest`) rather than always pointing to the `stable` release. ([#XXXX](https://github.com/mckinsey/vizro/pull/XXXX))
