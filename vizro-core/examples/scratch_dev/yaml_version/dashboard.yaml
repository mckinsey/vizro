# Still requires a .py to add data to the data manager and parse YAML configuration
# See yaml_version example
title: "Dashboard Example"
pages:
  - title: "Custom Bar Chart"
    components:
      - type: "graph"
        figure:
          _target_: "custom_charts.custom_bar"
          data_frame: "iris"
          x: "sepal_length"
          y: "sepal_width"
